/**
 * Contrato do índice de assets, lado TypeScript.
 *
 * Os tipos são gerados a partir do JSON Schema em `schemas/` assim que a Spec
 * (etapa 4) definir o contrato. Por enquanto só a versão é exportada.
 */
export const SCHEMA_VERSION = "0.0.0";
